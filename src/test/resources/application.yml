spring:
    datasource:
        url: "jdbc:h2:mem:contactmgr;db_close_on_exit=false"
        username: sa
        password:
    jpa:
        hibernate:
            ddl-auto: create-drop
        show_sql: true
        properties:
            hibernate:
                format_sql: true
            javax:
                persistence:
                    validation:
                        mode: none
    main:
        web-environment: false
        show_banner: false
